# 🚀 Запуск Django админки

## ✅ Быстрый старт

### 1. Активировать виртуальное окружение:

```bash
cd D:\projects\sparks
.\.venv\Scripts\Activate.ps1
```

### 2. Проверить/создать таблицы в БД:

```bash
cd backend
python scripts\check_tables.py
```

### 3. Применить Django системные миграции:

```bash
cd ..\admin
python manage.py migrate
```

### 4. Создать суперпользователя (если еще не создан):

```bash
python manage.py createsuperuser
```

Введите:
- Username: `admin`
- Password: (придумайте пароль)

### 5. Запустить админку:
docker-compose exec admin python manage.py createsuperuser
```bash
python manage.py runserver 8001
```

**Откройте:** `http://localhost:8001/admin/`

---

## 🔍 Проверка подключения

### Проверить таблицы в БД:

```bash
cd backend
python scripts\check_tables.py
```

### Проверить подключение Django к БД:

```bash
cd admin
python scripts\fix_admin.py
```

---

## 🚨 Решение проблем

### Ошибка: "no such table: completed_tasks"

**Решение:**
```bash
cd backend
python scripts\check_tables.py
```

Скрипт автоматически создаст все недостающие таблицы.

### Ошибка при запуске админки

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все таблицы созданы
3. Проверьте логи в консоли

---

## 📝 Важно

- Используйте виртуальное окружение: `.\.venv\Scripts\Activate.ps1`
- Админка работает на порту 8001 (бэкенд на 8000)
- Все таблицы создаются через Alembic в `backend/`
- Django только читает/редактирует данные (`managed = False`)
